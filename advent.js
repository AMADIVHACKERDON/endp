const prompt = require("prompt-sync")();

const answr = prompt("Would you like to play (y/n): ");

if (answr.toLowerCase() === "y") {
    const answr2 = prompt("Would you like to go left or go right (left/right)? ")
    if (answr2 == "left"){
        console
    }
}else {
    console.log("That's too bad!");
}